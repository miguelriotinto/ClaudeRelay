import Foundation
import CodeRelayKit

/// CodeRelayClient provides the client-side interface for connecting to a CodeRelay server.
public enum CodeRelayClient {
    public static let version = CodeRelayKit.version
}
